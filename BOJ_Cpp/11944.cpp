#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    string sNum;
    int mNum;

    cin >> sNum >> mNum;

    
        for(int j = 0; j < sNum.length(); j++){
            cout << sNum[j];
        }
    
    return 0;
}