#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int caseNum = 1;

    while(1){
        int n;
        cin >> n;

        if(n == 0) return 0;

        for(int i = 0; i < n; i++){
            int input;
            cin >> input;
        }

        cout << "Case " << caseNum <<": Sorting... done!\n";
        caseNum++;
    }


    return 0;
}