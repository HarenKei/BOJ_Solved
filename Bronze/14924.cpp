#include<bits/stdc++.h>

using namespace std;

int s, t, d;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    
    cin >> s >> t >> d;
    cout << d / (2 * s) * t << "\n";
    
    return 0;
}
