#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int ringo, mikan;
    const int banana = 3;

    cin >> ringo >> mikan;

    cout << (ringo + mikan + banana) << "\n";
}
