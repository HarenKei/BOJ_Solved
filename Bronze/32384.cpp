#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    string str = "LoveisKoreaUniversity";
    int n;

    cin >> n;

    for(int i = 0; i < n; i++) {
        cout << str << " ";
    }

    cout << "\n";
    
    return 0;
}