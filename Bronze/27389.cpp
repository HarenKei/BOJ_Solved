#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    float n;
    cin >> n;

    cout << fixed;
    cout.precision(2);

    cout << n / 4 << "\n";

    return 0;
}
