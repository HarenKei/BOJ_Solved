#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    long long n, m;

    cin >> n >> m;

    if(n != m){
        cout << 0 << "\n";
    } else {
        cout << 1 << "\n";
    }
    return 0;
}