#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n;
    cin >> n;

    if(n == 0){
        cout << "YONSEI\n";
    } else if(n == 1){
        cout << "Leading the Way to the Future\n";
    }

    return 0;
}