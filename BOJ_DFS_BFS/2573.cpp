#include <bits/stdc++.h>
#define MAX 301

using namespace std;

int n, m;
int sea[MAX][MAX];

void eraseIceberg(){
    int curX, curY, nextX, nextY;
    int tmpSea[MAX][MAX];

    for(int i = 0; i < )

}


int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    cin >> n >> m;

    for(int i = 0; i < n; i++){
        for(int j = 0; j < m; j++){
            cin >> sea[i][j];
        }
    }

    cout << endl;

    return 0;
}